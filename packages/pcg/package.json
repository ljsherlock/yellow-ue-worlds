{
  "name": "@yellow-ue/pcg",
  "version": "0.0.1",
  "private": true,
  "description": "The PCGRunner boundary — parameterize a Procedural Content Generation graph, get generated points. Phase 1 ships the interface + MockPCGRunner; the real UE PCG integration lands in Phase 2 Track D.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mock": "./src/mock.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint configured later' && exit 0",
    "format": "echo 'format configured later' && exit 0",
    "build": "echo 'pcg is consumed source-first; no build step' && exit 0"
  },
  "dependencies": {
    "@yellow-ue/tracing": "workspace:*",
    "@yellow-ue/world-api": "workspace:*",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
