{
  "name": "@yellow-ue/streaming",
  "version": "0.0.1",
  "private": true,
  "description": "The StreamingMetrics boundary — live Pixel Streaming health (codec, bitrate, FPS, RTT, packet loss). Phase 1 ships the interface + MockStreamingMetrics; real WebRTC stats land in Phase 3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mock": "./src/mock.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint configured later' && exit 0",
    "format": "echo 'format configured later' && exit 0",
    "build": "echo 'streaming is consumed source-first; no build step' && exit 0"
  },
  "dependencies": {
    "@yellow-ue/tracing": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
