{
  "name": "@yellow-ue/world-model",
  "version": "0.0.1",
  "private": true,
  "description": "The authoritative world-model relationship graph + deterministic behaviour simulation. Entities, typed relationships, dispositions, weather — and the per-tick sim that makes an ecosystem behave (flee/stalk/herd/drink/rest).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./store": "./src/store.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint configured later' && exit 0",
    "format": "echo 'format configured later' && exit 0",
    "build": "echo 'world-model is consumed source-first; no build step' && exit 0"
  },
  "dependencies": {
    "@yellow-ue/tracing": "workspace:*",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
