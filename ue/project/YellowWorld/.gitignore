# UE generated / derived — never commit these
Binaries/
Build/
Intermediate/
DerivedDataCache/
Saved/
*.VC.db
*.opensdf
*.sdf
*.sln
*.xcodeproj
.vs/

# The spike level is generated headlessly by Scripts/make_map.py at build time,
# so we don't commit the binary .umap.
Content/Maps/Spike.umap
